apiVersion: v1
kind: Service
metadata:
  name: mcp-server
  namespace: homeautomation
  labels:
    app: mcp-server
spec:
  selector:
    app: mcp-server
  ports:
  - name: http
    port: 80
    targetPort: 8086
  type: ClusterIP
