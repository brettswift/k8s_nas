apiVersion: v1
kind: Service
metadata:
  name: alertmanager-ntfy-bridge
  namespace: monitoring
  labels:
    app: alertmanager-ntfy-bridge
spec:
  selector:
    app: alertmanager-ntfy-bridge
  ports:
  - port: 9099
    targetPort: 9099
  type: ClusterIP
