apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # HTTP-01 challenge (no AWS needed, simpler setup)
  - clusterissuer-http.yaml
  - certificate-http.yaml
  # DNS-01 challenge (requires AWS Route53 credentials) - keeping for reference
  # - clusterissuer.yaml
  # - certificate.yaml

