resources:
  # - nvidia-device-plugin/  # Disabled - glibc incompatibility with host OS
  - cert-manager/
  - external-dns/
  - mcp-server/
  # openclaw/ and searxng/ are deployed by the Argo CD Application "openclaw"
  # (path apps/infrastructure/openclaw) to avoid duplicate resources with this app.
