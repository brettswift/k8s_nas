resources:
  - nvidia-device-plugin/
  - cert-manager/
  - traefik/
# argocd/ removed - managed by argocd-config Application separately
# monitoring/ removed - managed by monitoring Application separately
