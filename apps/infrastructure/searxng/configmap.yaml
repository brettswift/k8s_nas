apiVersion: v1
kind: ConfigMap
metadata:
  name: searxng-settings
  namespace: openclaw
data:
  settings.yml: |
    use_default_settings: false

    search:
      formats:
        - html
        - json
