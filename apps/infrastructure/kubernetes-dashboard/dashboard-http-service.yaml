apiVersion: v1
kind: Service
metadata:
  name: kubernetes-dashboard-http
  namespace: kubernetes-dashboard
  labels:
    k8s-app: kubernetes-dashboard
spec:
  ports:
  - port: 80
    targetPort: 8443
    protocol: TCP
  selector:
    k8s-app: kubernetes-dashboard
