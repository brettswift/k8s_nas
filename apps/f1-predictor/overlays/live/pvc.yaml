apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: f1-live-data
  namespace: f1-live
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 256Mi
  storageClassName: standard
