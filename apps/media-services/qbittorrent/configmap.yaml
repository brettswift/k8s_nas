apiVersion: v1
kind: ConfigMap
metadata:
  name: qbittorrent-init-scripts
  namespace: qbittorrent
data:
  # Note: qBittorrent's WebUI\RootFolder setting doesn't persist reliably
  # with linuxserver image. Using nginx rewrite-target instead.
  # This script is kept for potential future use.
  99-placeholder.sh: |
    #!/bin/bash
    echo "qBittorrent init - using nginx rewrite-target for /qbittorrent path"
